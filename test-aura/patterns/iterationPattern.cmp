<aura:component>
    <aura:attribute name="items" type="List" default="['A','B','C']"/>
    <aura:iteration items="{!v.items}" var="item">
        <div>{!item}</div>
    </aura:iteration>
</aura:component> 